---
cssclasses:
  - center-images
  - center-titles
---
Tags:  #docker

# Compose

- Docker compose allows us to run define and run multiple services in  1 isolated environment.


- Code that defines how your services should run
- Code can be versioned
- Easier Collabaration

### For networking in Compose file ? 
- By default, Compose sets up a single network for your app
- Communication via container name
- But option to specify your own networks with the top-level "networks" key
